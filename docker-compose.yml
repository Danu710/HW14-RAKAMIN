version: '3.1'

services:

  db:
    image: postgres
    container_name: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: danu123
      POSTGRES_DB: dbbook
    ports:
      - "5432:5432"

